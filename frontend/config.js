// Edit this before deploying to GitHub Pages.
// Point it at your deployed backend (e.g. Render) URL -- HTTPS, no trailing slash.
// This is NOT a secret -- it's just an address, safe to commit to a public repo.
window.APP_CONFIG = {
  BACKEND_URL: 'https://your-backend.onrender.com',
};
